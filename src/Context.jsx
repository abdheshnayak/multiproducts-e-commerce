import { createContext, useContext } from "react";

export const Context = createContext()

const ContProvider=()=>{
return (
    <Content.Provider value= "Moni">

        
    </Content.Provider> 
)
}